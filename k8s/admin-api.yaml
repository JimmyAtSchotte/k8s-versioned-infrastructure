apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-api-deployment  
  labels:
    app: admin-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-api
  template:
    metadata:
      labels:
        app: admin-api
    spec:
      containers:
        - name: admin-api
          image: localhost:5000/admin-api
          ports:
            - containerPort: 8080
          env:                        
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: secrets
                  key: admin_db_username
            - name: DB_PASSWORD
              valueFrom: 
                secretKeyRef:
                  name: secrets
                  key: admin_db_password
            - name: DB_SERVER
              valueFrom:
                configMapKeyRef:
                  name: config
                  key: admin_db_server
            - name: DB_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: config
                  key: admin_db_database
            - name: QUEUE_HOST
              valueFrom:
                configMapKeyRef:
                  name: config
                  key: queue_host
            - name: QUEUE_NAME
              valueFrom:
                configMapKeyRef:
                  name: config
                  key: queue_name                  
            - name: QUEUE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-deployment-default-user
                  key: username
            - name: QUEUE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-deployment-default-user
                  key: password
---
apiVersion: v1
kind: Service
metadata:
  name: admin-api-service
spec:
  selector:
    app: admin-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080